import Image from 'next/image'

const ProfilePhoto = () => {
  return (
    <div className="profile-photo">
      <h2>[Profile photo placeholder image]</h2>
      <button>Upload photo</button>
    </div>
  )
}

export default ProfilePhoto;